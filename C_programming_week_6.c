/*#include <stdio.h>

// 문자열 복사 프로그램
int main() {


	char src[] = "Action speaks louder than words."; // 원본 문자열 선언
	char dst[100]; // 복사할 배열 선언 (크기가 충분히 커야함)

	int i;
	printf("원본 문자열 = %s\n", src);
	for (i = 0; src[i] != '\0'; i++)
		dst[i] = src[i]; // src의 각 문자를 하나씩 dst로 복사
	
	dst[i] = '\0'; // 널 문자 (\0)전까지 복사 지정
	printf("복사된 문자열 = %s\n", dst);

	return 0;
}*/
