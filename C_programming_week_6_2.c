/*#include <stdio.h>

int main() {

	char str[30] = "C language is easy"; // 문자열 선언 + 초기화
	int i = 0;

	while (str[i] != 0) // 문자열 끝이 나올때까지 반복
		i++;

	printf("문자열 \"%s\"의 길이는 % d입니다.\n", str, i); // 길이 출력
	return 0;
}*/